#[derive(Debug)]
pub enum Opcode {
    Add,
    LoadConstant(usize),
    Print(usize),
    Pop,
    Abs,
    Nagetive,
    JumpNotTruthy(usize),
    Jump(usize),
    Null,
    ReturnValue,
    SetGlobal(usize),
    GetGlobal(usize),
    SetLocal(usize),
    GetLocal(usize),
    GetBuiltin(usize),
    Closure(usize, usize),
    GetFree(usize),
    CurrentClosure,
    Call(usize),
    Return,
    Array(usize),
    Hash(usize),
    Index,
    SetIndex,
    True,
    False,
    Equal,
    NotEqual,
    GreaterThan,
    LessThan,
    GreaterThanEqual,
    LessThanEqual,
    Minus,
    Divide,
    Multiply,
    Remainder,
    Bang,
    MinusOne,
    PlusOne,
    DefineGlobal(String),
}
